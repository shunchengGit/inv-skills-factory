# ── 外部知识库 ────────────────────────────────────────────────────────────
# INV_KNOWLEDGE_REPO_URL=           # Git 远程（默认 git@github.com:shunchengGit/inv-knowledge.git）
# INV_KNOWLEDGE_ROOT=               # 本地根目录（默认 ~/.inv-knowledge，研报归档在 res/）

# ── 部署目标 ──────────────────────────────────────────────────────────────
DEPLOY_SKILLS_DIR=inv-skills        # 必填；Hermes / WorkBuddy 下的部署子目录名
