# Proposal: meta-json-standardize

## Summary
统一所有 skill 的 `_meta.json` 格式，修正版本号不一致问题，补充缺失的 `_meta.json`。

## 问题
1. `_meta.json` 存在两套不兼容的 schema
2. `quality-growth-qarp` 缺少 `_meta.json`
3. `stock-research-report-analysis` 和 `tencent-leading-indicators` 版本号与 SKILL.md 不一致
