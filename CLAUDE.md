# CLAUDE.md

本仓库的项目指导、架构边界、数据源约束与部署约定以 [`AGENTS.md`](AGENTS.md) 为唯一权威来源。

开始任何代码、技能或部署工作前，必须先读取并遵守 `AGENTS.md`。不要在本文件复制投资分析分层、数据源/代理边界、部署流程、harness 路径等易变事实；这些只在 `AGENTS.md` 及其引用的权威文档（各技能 `SKILL.md`、`openspec/config.yaml`）中维护，避免双份文档漂移。
